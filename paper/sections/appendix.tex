% Full per-platform figure sets and anything the page budget pushes out.
\section{Full scaling grids}
% F2b: the referee's is-it-a-D=8-artifact question, answered from the sweeps.
\begin{figure*}[p]
  \centering
  \includegraphics[width=.85\textwidth]{f2b-crossover-vs-d.png}
  \caption{The contraction ratio against device count, every strong-scaling
    cell. B's advantage over the materializing and seed-regenerated arms grows
    monotonically with $D$ on both platforms; the low-rank family stays within
    a few percent of parity throughout. The $D{=}8$ pattern of
    Figure~\ref{fig:f2} is the endpoint of a monotone trend, not an artifact.}
  \label{fig:f2b}
\end{figure*}

% M1/M2/M3 per platform -- the appendix carries what F1/F2 summarize.
% Explicit paths: the A100 and TPU sets share filenames, so \graphicspath
% would silently pick whichever directory is listed first.
\begin{figure*}[p]
  \centering
  \includegraphics[width=.9\textwidth]{../experiments/phase2/figures/m1-strong-scaling.png}
  \caption{Strong scaling, all cells, 8$\times$A100.}
\end{figure*}
\begin{figure*}[p]
  \centering
  \includegraphics[width=.9\textwidth]{../experiments/phase2/figures-tpu-v5e8/m1-strong-scaling.png}
  \caption{Strong scaling, all cells, TPU v5e-8. The dense arm has no
    $D{=}1$ point at $d{=}2048$, $N{=}256$ (out of memory on one 16\,GB
    chip), so its efficiency there is normalized to its smallest measured
    device count, $D{=}2$.}
\end{figure*}
\begin{figure*}[p]
  \centering
  \includegraphics[width=.9\textwidth]{../experiments/phase2/figures/m2-weak-scaling.png}
  \caption{Weak scaling, all cells, 8$\times$A100.}
\end{figure*}
\begin{figure*}[p]
  \centering
  \includegraphics[width=.9\textwidth]{../experiments/phase2/figures-tpu-v5e8/m2-weak-scaling.png}
  \caption{Weak scaling, all cells, TPU v5e-8.}
\end{figure*}
\begin{figure*}[p]
  \centering
  \includegraphics[width=.9\textwidth]{../experiments/phase2/figures/m3-crossover.png}
  \caption{Contraction crossover per cell, 8$\times$A100: B wins 10 of 16
    cells.}
\end{figure*}
\begin{figure*}[p]
  \centering
  \includegraphics[width=.9\textwidth]{../experiments/phase2/figures-tpu-v5e8/m3-crossover.png}
  \caption{Contraction crossover per cell, TPU v5e-8: B wins everywhere for
    the dense and seed-regenerated arms, while A leads for both low-rank
    strategies at $d{=}2048$. The platform moves the crossover, which is why
    it is reported as a measured surface rather than a rule.}
\end{figure*}

\section{Reproduction}
Every number in the paper regenerates from a committed script against a
committed results directory, and every results directory's README records
the exact session that produced it (platform, commit, environment, cost,
and any incident). The figure and table map: F1, F2 and F2b from
\texttt{plot\_paper.py}; F4 from \texttt{plot\_cost.py}; F5 from the
phase-0 plots; F7 from \texttt{plot\_e13.py}; F8 from
\texttt{plot\_e14.py}; the appendix grids (M1--M3, both platforms) from
\texttt{plot.py} against each platform's results directory; Tables~1--3 from \texttt{tb1.py},
\texttt{tb2.py} and \texttt{tb3.py} with \texttt{--latex}, which write
the exact files this document inputs. The test suite runs on CPU with no
network; the multi-device logic is exercised on eight simulated devices, so
correctness properties (including the device-count invariance test) are
checkable without any accelerator, and the two TPU compile probes
(\texttt{probe\_lr1.py}, \texttt{probe\_eggroll\_tpu.py}) reproduce
the compiler-level claims from any Linux machine.
%% TODO(andres): repo URL, commit or tag of the submitted version, and the
%% one-line clone-and-build once the artifact is frozen.
